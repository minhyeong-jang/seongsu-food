import { FC } from "react";
import styled from "@emotion/styled";

export const NaverFoodPriceCollectionContainer: FC = () => {
  return <StyledContainer></StyledContainer>;
};
const StyledContainer = styled.div`
  padding: 0;
`;
